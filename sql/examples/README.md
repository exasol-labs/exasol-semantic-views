# SQL Examples

Example files:

- `sales_physical_model.sql`
- `sales_model_seed.sql`
- `sales_semantic_queries.sql`
- `sales_osi.yaml`

Run `sales_physical_model.sql` before `sales_model_seed.sql`.

`sales_osi.yaml` is a Milestone 0 OSI `0.2.0.dev0` interoperability fixture for
the sales model. It is an import/export example, not an install script.
