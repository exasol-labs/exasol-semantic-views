interface:
  display_name: "Exasol Semantic Modeler"
  short_description: "Build, import, and maintain Exasol semantic models"
  default_prompt: "Use $exasol-semantic-modeler to inspect the physical schema, import Databricks UCMV YAML when applicable, derive a semantic model, author metrics, validate, and publish an Exasol Semantic Views model."
policy:
  allow_implicit_invocation: false
