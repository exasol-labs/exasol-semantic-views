interface:
  display_name: "Exasol Semantic Modeler"
  short_description: "Build, import, and maintain Exasol semantic models"
  default_prompt: "Use $exasol-semantic-modeler to inspect schema comments and structure, derive and author a semantic model, validate it, and publish an Exasol Semantic Views model."
policy:
  allow_implicit_invocation: false
